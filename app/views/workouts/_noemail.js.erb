<% flash[:danger] = "Email unable to send.  #{Workout.find(params[:id]).appointment.client.name} must have a valid email address first." %>
$("#flash_msg").html("<%= escape_javascript(render('workouts/flmsg')) %>");