
                    
                    <%= form_for(Exercise.new, html: { :class => "form-horizontal"}, remote: true) do |f| %>
                    <% if @exercise.errors.any? %>
                        <div id="error_explanation">
                          <h2><%= pluralize(@exercise.errors.count, "error") %> prohibited this exercise from being saved:</h2>

                          <ul>
                          <% @exercise.errors.full_messages.each do |msg| %>
                            <li><%= msg %></li>
                          <% end %>
                          </ul>
                        </div>
                      <% end %>

                        <div class="form-group">
                              <%= f.label :name, :class => "control-label", :style => "width:100%; text-align:center;" %>
                              <div class="col-md-8">
                              <%= f.text_field :name, :class => "form-control", :placeholder => " ", :style => "border-color: #d0dde9; width:286px; text-align:center;" %>
                            </div>
                        </div>

                         <div class="form-group">
                          <% the_array2 = ["Reps", "Distance", "Time"] %>
                          <%= label_tag :exercise, "Measuring", :class => "control-label", :style => "width:100%; text-align:center;" %>
                          <div class="col-md-8 ui-select" id="oder" style="min-width:286px; width:286px;left: 14px;">
                          <%= f.select :measure, options_for_select(the_array2.map { |value| [ value, value ] }), {:prompt => "Must Choose One"}, {:class => "form-control", :style => "padding-left: 12px; width: 107%;text-indent:72px;"} %>
                        </div>
                        </div>

                        <div class="form-group">
                              <%= f.label :description, :class => " control-label", :style => "width:100%; text-align:center;" %>
                              <div class="col-md-8">
                              <%= f.text_field :description, :class => "form-control", :placeholder => " ", :style => "border-color: #d0dde9;width:286px; text-align:center;" %>
                            </div>
                        </div>

                       

                        <div class="form-group">
                            <div style="width:100%; text-align:center;">
                              <%= f.submit "Create My Exercise", class: "btn mybtn btn-large btn-primary" %>
                            </div>
                        </div>
                              <% end %>


                </div>

                
            






